<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>KPRP</title>
  <link rel="stylesheet" href="/style.css">
  <script src="https://kit.fontawesome.com/d3aa2aab48.js" crossorigin="anonymous"></script>
</head>
<body>
  <header>
    <div class="navbar-logo">
      <span>
        <img src="/images/Logosvg.svg" style="width:30px; height: 20px;" />
      </span> 
      KPRP
    </div>
    <nav>
      <ul>
        <% if (isAdmin) { %>
          <li>
            <a href="/admin/dashboard"  class="<%= (activePage === 'dashboard') ? 'active' : '' %>">Admin Dashboard</a>
          </li>
          <li>
            <form action="/admin/logout" method="POST" style="display:inline;">
              <button type="submit" class="logout_btn">
                <i class="fa-solid fa-right-from-bracket"></i> Logout Admin
              </button>
            </form>
          </li>
          <% } else { %>
            <li><a href="/" class="<%= (activePage === 'home') ? 'active' : '' %>"><i class="fa-solid fa-house"></i> Home</a></li>
            <li><a href="/admin/login" class="<%= (activePage === 'adminLogin') ? 'active' : '' %>"><i class="fa-solid fa-user"></i> Admin</a></li>
            <% } %>
      </ul>
    </nav>
  </header>
